## 1. 决策确认

- [x] 1.1 与设计/程序确认稳定值模型：默认采用 `baseStability + 距离衰减`，返回范围为 `0-10`
- [x] 1.2 确认 Gym MVP 索引方案：优先使用近似攀爬平面 cell 分桶加速，最终距离、半径和稳定值使用 3D 世界坐标校验
- [x] 1.3 确认抓取半径直接使用现有 `previewSlightRadius`，保持手感探测和实际抓取逻辑一致
- [x] 1.4 确认最近抓点接口是否默认带 `maxDistance`，避免远处抓点触发误导反馈

## 2. 数据模型与接口

- [x] 2.1 盘点现有 `Anchor Point` 脚本和 Gameplay 初始化入口，确定复用、迁移或新增位置
- [x] 2.2 定义 `Anchor Point` 手动配置字段：基础稳定值，并复用现有 `previewSlightRadius` 作为抓取半径
- [x] 2.3 定义运行时抓点数据结构，保存自动分配 ID、世界坐标、基础稳定值、来自 `previewSlightRadius` 的抓取半径和来自 `gameObject.name` 的调试名
- [x] 2.4 定义关卡抓点查询接口，至少包含稳定值查询和最近真实抓点查询
- [x] 2.5 定义结构化查询结果，包含是否命中、抓点 ID、调试名、坐标、距离、基础稳定值、当前稳定值和半径

## 3. 初始化与索引

- [x] 3.1 在 Gameplay 初始化阶段收集 Gym 场景中启用且合法的 `Anchor Point` 节点
- [x] 3.2 实现初始化校验，覆盖稳定值范围、`previewSlightRadius`、自动 ID 分配、禁用点和无效点警告
- [x] 3.3 构建运行时只读抓点快照，确保查询热路径不直接依赖场景层级扫描
- [x] 3.4 实现空间哈希索引，用于根据世界点快速缩小候选抓点集合
- [x] 3.5 提供索引配置参数，至少包含 cell size、近似攀爬平面来源、邻域扩张范围和默认抓取半径

## 4. 查询实现

- [x] 4.1 实现稳定值查询：不可抓取返回 `0`，命中真实抓点时返回 `0-10` 范围内的距离衰减稳定值
- [x] 4.2 实现最近抓点查询：返回最近合法真实抓点或明确空结果
- [x] 4.3 实现可选最大搜索距离过滤，超出距离时返回空结果
- [x] 4.4 确保平面 cell 分桶只用于缩小候选集合，最终命中、距离、半径和稳定值均按 3D 世界坐标计算
- [x] 4.5 确保查询路径不调用 `FindObjectsOfType`、不扫描场景层级、不创建不必要的临时对象
- [x] 4.6 保持关卡系统只返回抓点事实，不硬编码受力系统的最小抓附阈值、稳定阈值或脱力结果

## 5. Gameplay 与调试接入

- [x] 5.1 将关卡抓点查询服务接入 Gameplay 初始化流程，使其他玩法模块可获取同一查询实例
- [x] 5.2 为受力适配器或临时测试调用方提供读取抓点 ID、稳定值和距离的接入点
- [x] 5.3 增加初始化调试输出，显示注册抓点数量、跳过数量和主要校验警告
- [x] 5.4 增加查询调试输出或 Gizmos，显示输入点、命中抓点、距离、基础稳定值和返回稳定值

## 6. 验收与测试

- [x] 6.1 编写初始化测试，验证合法抓点进入快照、禁用或非法抓点被跳过
- [x] 6.2 编写稳定值查询测试，覆盖不可抓取返回 `0`、中心点高稳定、边缘距离衰减和范围限制
- [x] 6.3 编写最近抓点查询测试，覆盖最近点选择、无抓点空结果和最大搜索距离过滤
- [x] 6.4 编写起伏岩壁查询测试或场景验证，确认平面分桶候选最终通过 3D 世界距离校验
- [x] 6.5 编写性能边界测试或调试验证，确认高频查询不触发场景全量扫描
- [x] 6.6 在 Gym 或轻量测试场景中完成手工冒烟：Gameplay 初始化、查询命中、空结果、调试可视化均符合规格
