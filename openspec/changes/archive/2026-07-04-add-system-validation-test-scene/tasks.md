## 1. 场景与样例数据

- [x] 1.1 对照 `Assets/DesignerSpace/Gym.scene` 梳理可复用的摄像机、攀爬平面、抓点组件和层级命名方式
- [x] 1.2 新建独立的系统验证测试场景，命名为 `SystemValidationTest` 或同等清晰名称，并保持与 Gym 主场景隔离
- [x] 1.3 在测试场景中配置攀爬平面、测试摄像机和输入射线可命中的墙面 Collider 或测试 Plane
- [x] 1.4 在测试场景中布置高稳定有效抓点、低稳定抓点和无抓点空区域，并使用与 Gym 兼容的抓点数据组件
- [x] 1.5 为后续障碍点和假点样例预留区域或占位说明，但不阻塞第一版基础验证

## 2. 临时输入点控制

- [x] 2.1 实现测试输入点数据结构，分别记录左手和右手的屏幕坐标、世界坐标、是否接触测试平面和当前有效性
- [x] 2.2 实现 PC 鼠标点击/拖动输入，将屏幕坐标通过摄像机射线映射到测试攀爬平面
- [x] 2.3 实现移动端触摸点击/拖动输入，默认左半屏控制左手、右半屏控制右手，并支持双指同时拖动
- [x] 2.4 处理射线未命中、触摸结束和输入取消，将对应手标记为未接触并清理旧的有效坐标
- [x] 2.5 在场景中显示左右手测试输入点标记，便于观察输入映射是否正确

## 3. 关卡查询与受力链路

- [x] 3.1 在测试场景初始化时接入关卡抓点系统，确认场景抓点可被注册、索引和查询
- [x] 3.2 为左右手输入点调用关卡抓点查询，获取命中状态、抓点 ID 或调试名、距离、稳定值和空结果
- [x] 3.3 实现 `ValidationForceInputAdapter` 或同等测试适配器，将输入点、关卡查询结果和临时耐力状态组装为受力输入快照
- [x] 3.4 接入受力评估器，实时输出整体受力状态、左右手有效性、失败原因和坠落触发事实
- [x] 3.5 验证测试场景不新增第二套正式受力规则，所有稳定、脱力和坠落结论均来自受力系统链路

## 4. PC 调试 UI 与可视化

- [x] 4.1 实现 PC 调试 UI，显示左右手输入点坐标、接触状态、命中抓点、距离和稳定值
- [x] 4.2 在 UI 中显示整体受力状态、左右手有效性、主要失败原因、最近状态切换和关键受力参数
- [x] 4.3 为输入点、命中抓点、查询半径和空结果添加 Gizmo、LineRenderer 或等效可视化标记
- [x] 4.4 为高频刷新 UI 做基础节流或固定字段刷新，避免通过 Console 高频刷屏作为主要观察方式

## 5. 移动端震动反馈

- [x] 5.1 封装移动端震动反馈适配层，支持关闭、二值震动和可用平台上的强度或脉冲映射
- [x] 5.2 将有效抓点和受力稳定程度映射为震动强度、脉冲频率或持续时间
- [x] 5.3 在未触摸、无抓点、稳定值为 `0`、触摸取消或对应手无效受力时停止震动
- [x] 5.4 在移动端设备或可用模拟路径上验证无抓点不震动、稳定越高反馈越强、触摸结束无震动残留

## 6. 验收与稳定性检查

- [x] 6.1 手工验证双手高稳定抓点输出稳定状态，并在 PC UI 中显示正确抓点和稳定值
- [x] 6.2 手工验证单手空区域、双手空区域和低稳定抓点分别输出预期失败原因或脱力状态
- [x] 6.3 手工验证输入点从抓点中心拖动到边缘外时，距离、稳定值、命中状态、受力状态和震动反馈连续变化
- [x] 6.4 手工验证短暂离开抓点后恢复的状态缓冲行为，以及持续双手失效后的坠落触发行为
- [x] 6.5 重复进入 Play Mode 或重新加载测试场景，确认关卡查询、受力评估、UI 和震动反馈能正确初始化和清理
- [x] 6.6 检查高频拖动下无空引用异常、明显卡顿、异常日志刷屏或震动状态卡死
