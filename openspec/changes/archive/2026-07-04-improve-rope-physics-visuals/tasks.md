## 1. 配置与资源

- [x] 1.1 梳理现有 `RivetRopeConfig`、`RivetRopeLineVisual` 和安装器中的绳索表现参数，确认哪些保留、提升到配置或拆分为视觉配置。
- [x] 1.2 增加绳索视觉配置字段，覆盖宽度、材质、纹理平铺、松弛下垂、阻尼跟随、摆动、约束迭代和张力反馈参数。
- [x] 1.3 创建或配置默认绳索材质/占位纹理，使主玩法和验证场景不再使用默认纯色细线表现。

## 2. 默认程序化绳索表现

- [x] 2.1 增强现有 `LineRenderer` 绳索表现，使松弛状态呈现可调下垂、弧线和轻微摆动。
- [x] 2.2 增强拉紧状态反馈，使绳索在达到绳长约束时减少松弛并显示颜色、宽度或等价张力变化。
- [x] 2.3 增加端点和铆钉转折点的阻尼跟随，避免端点移动时整条绳索刚性跳变。
- [x] 2.4 根据绳索渲染长度或路径长度设置材质纹理平铺，避免长绳段明显拉伸。

## 3. 可选物理绳段模式

- [x] 3.1 设计并实现可选物理绳段组件，读取逻辑绳路和端点 Transform，本地生成表现用绳段点。
- [x] 3.2 支持用 Verlet/位置约束或等价轻量方案固定首尾和铆钉转折点，并提供基础重力、阻尼和迭代参数。
- [x] 3.3 确保可选物理绳段只写入表现数据，不修改核心 `RivetRopeModel`、库存、绳长、保护判定或伤害结果。
- [x] 3.4 记录 FImpossible Rope Swing 示例中可借鉴的挂点、ragdoll 摆荡和输入冲量思路，明确是否需要后续独立变更接入角色受力。

## 4. 专用视觉测试场景

- [x] 4.1 更新主玩法安装器，为绳索对象设置默认材质、宽度、视觉配置和必要组件引用。
- [x] 4.2 新增或重建 `RivetRopeVisualLab.scene`，使用简洁背景、固定观察镜头、距离刻度和两名模拟玩家腰部端点。
- [x] 4.3 为测试场景提供端点自动往返移动、按钮驱动移动或手动拖动能力，用于观察绳索跟随、阻尼和回弹。
- [x] 4.4 为测试场景提供直连、单铆钉转折、多铆钉转折和急转弯路径预设。
- [x] 4.5 在测试场景 UI 中显示当前表现模式、逻辑路径长度、剩余松弛量、张力状态、渲染点数量和关键视觉参数。
- [x] 4.6 支持在测试场景中切换轻量程序化表现和可选物理绳段表现。
- [x] 4.7 支持在测试场景中快速调整绳长、下垂强度、阻尼、摆动、宽度、材质或颜色等关键参数。

## 5. 测试与验收

- [x] 5.1 扩展或新增 EditMode 测试，验证表现模式和材质参数变化不影响库存、绳长约束、保护判定和伤害结果。
- [x] 5.2 执行现有 `RivetRopeSystemTests`，确认既有规则测试继续通过。
- [x] 5.3 在 Tuanjie/Unity 的 `RivetRopeVisualLab` 中手工检查松弛、拉紧、端点移动、铆钉转折、跟随回弹和材质平铺效果。
- [x] 5.4 记录物理绳段模式的性能和稳定性风险，如不达标则保持默认关闭并保留程序化表现作为主路径。

## 6. 主玩法接入修正

- [x] 6.1 将主玩法绳索端点优先绑定到本地和远端角色骨骼节点，失败时才回退到 `TorsoCenter` 或角色根节点。
- [x] 6.2 移除主玩法端点默认 SmoothDamp 延迟，使骨骼挂点严格贴合角色骨骼节点。
- [x] 6.3 稳定绳索渲染层级，避免透明排序导致绳索有时在角色前、有时在角色后。
- [x] 6.4 重新安装主玩法绳索对象到 `MainLevel.scene`，确保玩法场景使用新的视觉配置和绑定逻辑。
- [x] 6.5 运行 Unity 编译、`RivetRopeSystemTests` 和主玩法场景冒烟检查。
