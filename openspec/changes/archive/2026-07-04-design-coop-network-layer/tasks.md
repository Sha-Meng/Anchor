## 1. 服务器可用性与部署准备

- [x] 1.1 记录 `43.156.16.10` 当前 ping、TCP 端口和 HTTP 响应检查结果。
- [ ] 1.2 确认腾讯云服务器 SSH 登录方式、系统版本、CPU 架构、可用内存和可部署目录。
- [x] 1.3 确认 MVP WebSocket 服务使用 `80` nginx 反代还是单独测试端口。
- [x] 1.4 若使用单独测试端口，确认腾讯云安全组和系统防火墙已放行该端口。
- [x] 1.5 记录 HTTPS/WSS 暂不部署的限制：仅用于内部 MVP，不承载账号密码、支付、正式 token 或隐私数据。
- [x] 1.6 编写服务器部署指引，覆盖 SSH 登录、服务目录、运行时依赖、端口、nginx 反代、systemd 保活和日志检查。

## 2. 玩家身份与协议框架

- [x] 2.1 定义 MVP 身份策略：服务器分配临时 playerId，客户端本地保存可选 clientId，不提供昵称设置交互。
- [x] 2.2 在客户端实现本地 clientId 生成和持久化，不读取平台设备 ID。
- [x] 2.3 定义统一 JSON 消息信封字段：type、requestId、roomId、senderId、seq、sentAt、schema、payload。
- [x] 2.4 定义消息命名空间：system、room、game，并记录新增消息类型的命名规则。
- [x] 2.5 定义 payload 扩展规则，确保新增玩法字段不需要修改服务器转发逻辑。
- [x] 2.6 定义服务器需要解析的最小字段和不解析的玩法 payload 边界。
- [x] 2.7 编写协议示例，至少包含欢迎消息、房间列表、创建房间、加入房间、房主开始、角色状态和动作事件。

## 3. 房间交互流程

- [x] 3.1 实现服务器内存房间列表，包含 roomId、hostId、players、state、createdAt、lastActiveAt。
- [x] 3.2 实现 `room.list` 和 `room.list.result`，只返回未满员且处于 lobby 的房间。
- [x] 3.3 实现 `room.create` 和 `room.created`，创建者自动成为房主并进入房间界面。
- [x] 3.4 实现 `room.join` 和 `room.joined`，支持通过房间码或房间列表加入。
- [x] 3.5 实现 `room.updated`，在创建、加入、离开和状态变化时向房间内玩家广播。
- [x] 3.6 实现 `room.start` 和 `room.starting`，仅允许满员房间的房主开始游戏。
- [x] 3.7 实现 `room.enteredGame` 和 `room.inGame`，用于客户端切场景后的确认和游戏同步开始。
- [x] 3.8 实现 `room.leave` 和 `room.peerLeft`，处理玩家离开或连接断开。
- [x] 3.9 实现 `system.error`，覆盖房间不存在、房间已满、非房主开始、状态不允许和消息格式错误。

## 4. 轻量 Relay 服务端

- [x] 4.1 创建最小服务端项目，提供 WebSocket 连接入口。
- [x] 4.2 连接建立后发送 `system.welcome`，为客户端分配 playerId。
- [x] 4.3 校验消息 JSON 格式、type、房间归属、房间状态和消息大小。
- [x] 4.4 对允许转发的 `game.state`、`game.event` 或后续自定义玩法消息保留 payload 原样转发。
- [x] 4.5 实现基础日志，记录连接、创建房间、加入房间、开始游戏、离开房间和非法消息。
- [x] 4.6 在本机启动服务并用 WebSocket 调试工具验证完整房间流程。

## 5. 前端协议配置与规范文档

- [x] 5.1 设计 Unity/Tuanjie 客户端可编辑 JSON 协议定义配置，支持消息类型、schema、payload 字段说明、发送频率和调试显示名。
- [x] 5.2 让 `game.*` 玩法消息使用统一信封和自定义 payload，新增 payload 字段时无需修改服务器。
- [x] 5.3 生成工程协议规范文档 `docs/coop-network-protocol.md`，记录身份策略、消息信封、消息目录、房间流程和部署指引。
- [x] 5.4 创建项目 skill，指导后续维护协议规范、客户端协议配置和 relay 边界。
- [x] 5.5 生成 JSON 协议配置示例 `docs/coop-network-protocol.config.json`，作为 demo 场景的默认配置参考。
- [x] 5.6 确保协议字段或消息类型变更时同步更新规范文档和客户端 JSON 协议配置。
- [x] 5.7 验证只修改 `game.*` payload 字段、schema 示例、发送频率或调试显示名时，不需要重新部署服务器。

## 6. 腾讯云部署

- [x] 6.1 在服务器创建 relay 服务目录，例如 `/opt/anchor-relay`。
- [x] 6.2 上传或拉取 relay 服务代码并安装运行时依赖。
- [x] 6.3 启动 relay 服务监听 `127.0.0.1:8080`、`0.0.0.0:8080` 或最终选定端口。
- [ ] 6.4 如果使用 nginx 反代，配置 `/ws` 转发到 relay 服务并支持 WebSocket upgrade。
- [x] 6.5 从开发机连接 `ws://43.156.16.10/ws` 或 `ws://43.156.16.10:<port>`，确认收到 `system.welcome`。
- [ ] 6.6 使用 systemd 或简单进程管理脚本保持 relay 服务运行。
- [ ] 6.7 查看服务日志，确认房间列表、创建房间、加入房间、房主开始和消息转发均有记录。

## 7. Unity/Tuanjie 客户端接入

- [x] 7.1 创建客户端网络连接组件，支持连接、断开、发送和接收 WebSocket 消息。
- [x] 7.2 实现统一消息信封的序列化和反序列化。
- [x] 7.3 创建房间列表/房间调试 UI，支持刷新列表、创建房间、输入房间码、加入房间和离开房间。
- [x] 7.4 创建简单房间界面，显示房主、玩家数量、房间码和开始按钮。
- [x] 7.5 实现只有房主且房间满员时开始按钮可用。
- [x] 7.6 收到 `room.starting` 后显示倒计时或立即切换到游戏场景。
- [x] 7.7 进入游戏场景后发送 `room.enteredGame`，收到 `room.inGame` 后开始游戏同步。
- [x] 7.8 将本地玩家状态按固定频率放入自定义 payload 并发送 `game.state`。
- [x] 7.9 创建远端玩家表现对象，收到对方 `game.state` 后按 payload 更新表现。
- [x] 7.10 支持自定义 `game.event`，用于后续玩法动作事件扩展。
- [x] 7.11 创建基础网络 demo 场景入口，用于验证连接、建房、加入房间、开局和自定义协议交互。
- [x] 7.12 demo 场景读取或对齐 JSON 协议配置，使用配置中的 `game.state` 和 `game.event` 示例完成收发。

## 8. Demo 场景端到端验收

- [ ] 8.1 使用两个客户端验证房间列表初始为空或显示可加入房间。
- [ ] 8.2 验证玩家 A 创建房间后进入简单房间界面，玩家 B 能从列表或房间码加入。
- [ ] 8.3 验证房间未满时开始按钮不可用，满员后房主可点击开始。
- [ ] 8.4 验证非房主发送开始请求会收到错误。
- [ ] 8.5 验证房主开始后双方能进入同一个 demo 游戏场景，并开始互相转发 `game.state`。
- [ ] 8.6 验证 demo 场景内本地玩家状态能通过 `game.state` payload 驱动远端表现。
- [ ] 8.7 验证 demo 场景内至少一个自定义 `game.event` 能端到端发送、转发、接收并在 UI 或日志中显示。
- [ ] 8.8 验证修改 JSON 协议配置中的 `game.*` payload 示例或发送频率后，不需要重新部署服务器即可完成客户端侧验证。
- [ ] 8.9 验证房间满员、无效房间码、玩家离开和服务端错误日志。
- [ ] 8.10 调整状态同步频率和远端插值参数，使 MVP 体验达到可演示水平。
