## 1. 数据模型与配置

- [x] 1.1 确认 `Assets/Anchor/` 下铆钉和绳索系统的脚本目录与命名，避免与关卡 `AnchorPoint` 混淆。
- [x] 1.2 定义玩法铆钉运行时数据，覆盖铆钉 ID、携带者、部署者、部署位置、携带/已部署状态和调试名。
- [x] 1.3 定义玩家铆钉库存数据，保证玩家携带数量和场上部署数量总和守恒。
- [x] 1.4 新增 ScriptableObject 配置，包含总铆钉数量、默认 20 米绳索总长度、插锚范围、回收范围、收绳救援窗口和伤害参数。

## 2. 核心规则实现

- [x] 2.1 实现插锚规则：合法候选位置和玩家库存满足条件时消耗携带铆钉并部署玩法铆钉。
- [x] 2.2 实现插锚失败原因：无库存、非法位置、不可交互状态和其他规则拒绝均返回结构化结果。
- [x] 2.3 实现拔锚和回收规则：玩家靠近已部署铆钉且处于 stable 时显示回收按钮，点击后直接回收并将库存加入回收玩家。
- [x] 2.4 实现领攀/跟攀身份状态和安全换领入口，保持两名玩家库存不被远程转移。
- [x] 2.5 实现绳索路径计算，按两名玩家端点和已部署铆钉生成逻辑路径长度。
- [x] 2.6 实现绳长约束输出，区分松弛、拉直、剩余松弛量和超出约束距离，并保证插锚不增加总绳长。
- [x] 2.7 实现坠落保护结算，选择最高已部署保护铆钉并输出受保护或无保护结果。
- [x] 2.8 实现主动收绳规则：救援窗口内校验下方玩家状态、累计有效点击、计算收绳量和减伤上限。
- [x] 2.9 实现伤害结果计算，按保护状态、估算自由坠落距离和有效收绳量输出建议伤害值与原因。
- [x] 2.10 实现回收参与绳路铆钉后的即时重算，覆盖保护点变更、保护消失和伤害变化。
- [x] 2.11 为打钉成功和回收成功生成可同步事件数据，包含事件 ID、操作者、铆钉 ID、位置或被回收 ID、操作后库存和绳路版本号。

## 3. 系统接入

- [x] 3.1 接入角色 3C 或交互适配器提供的插锚/拔锚/收绳请求、玩家位置、墙面命中和合法交互事实。
- [x] 3.2 接入受力系统输出的坠落触发事实，驱动铆钉和绳索坠落保护结算。
- [x] 3.3 向角色 3C 输出绳索拉直、约束距离、救援窗口、受保护坠落和无保护坠落状态。
- [x] 3.4 向生命系统或生命适配器输出结构化伤害结果，保持最终扣血逻辑在生命系统内。
- [x] 3.5 为绳索视觉提供只读路径点和剩余松弛量，使 LineRenderer、绳圈或 UI 松弛提示不参与玩法判定。
- [x] 3.6 为下方玩家提供收绳按钮高亮、有效点击反馈和无效点击反馈的最小 UI/表现接入。
- [x] 3.7 接入网络 `game.event` 发送和接收：成功打钉发送 `rivet.place`，成功回收发送 `rivet.collect`，远端按 `eventId` 去重并更新只读铆钉、库存和绳路表现。
- [x] 3.8 合入 `MainLevel` 主玩法验证：运行时被动绑定 3C 躯干位置和绳索端点，不安装调试输入控制器，不干扰主玩法输入。
- [x] 3.9 在 `MainLevel` 接入正式 UGUI 操作入口：插锚、回收铆钉和收绳救援按钮按当前库存、回收范围和救援窗口刷新可见状态，不常驻显示不可执行入口。
- [x] 3.10 将主玩法绳索两端绑定到角色腰部挂点：本地端跟随 3C 躯干腰部偏移，双人时远端端点跟随 `Remote Climber` 腰部，并通过墙面/侧边表现偏移避免绳索从角色正前方穿插。

## 4. 调试与验收

- [x] 4.1 新增调试输出或调试面板，显示两名玩家库存、场上铆钉、领攀关系、绳索状态、剩余松弛量、救援窗口、有效收绳量和最近坠落结算。
- [x] 4.2 编写纯规则测试或编辑器测试，覆盖库存守恒、合法插锚、非法插锚、stable 回收、非 stable 不可回收、安全换领和收绳状态校验。
- [x] 4.3 编写绳索规则测试，覆盖无锚直连、有锚路径、插锚不增总长、绳索松弛、绳索拉直和回收后路径更新。
- [x] 4.4 编写坠落结算测试，覆盖无保护坠落、有保护坠落、最高保护铆钉选择、回收保护铆钉后重算、主动收绳减伤和伤害随距离不降低。
- [x] 4.5 编写事件同步测试或双客户端手工验收，覆盖 `rivet.place`、`rivet.collect`、重复事件去重、乱序旧事件忽略或按版本处理。
- [x] 4.6 在 Unity/Tuanjie 测试场景中完成 MVP 冒烟验收：插锚消耗、拔锚回收、事件同步、绳长拉直、主动收绳、受保护坠落、无保护坠落和领攀交替。
- [x] 4.7 在 `MainLevel` 主玩法场景中完成被动绳索冒烟验收：3C 运行时绑定、无额外输入监听、无可见调试探针和 FImpossible 示例风格的非硬直绳索表现。
- [x] 4.8 在 `MainLevel` 主玩法场景中完成正式 UI 和腰部挂点冒烟验收：UI 条件显示、无调试输入组件、绳索从角色腰侧附近引出、Console 无错误。
